<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cảm ơn Thanh Hương!</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background: url('https://www.example.com/your-romantic-background.jpg') no-repeat center center fixed;
            background-size: cover;
            color: white;
            text-align: center;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            animation: backgroundAnimation 5s infinite alternate;
            overflow: hidden;
        }

        h1 {
            font-size: 3.5em;
            letter-spacing: 2px;
            animation: titleAnimation 3s ease-in-out infinite;
        }

        .heart {
            font-size: 3em;
            color: red;
            animation: heartAnimation 1s infinite alternate;
        }

        p {
            font-size: 1.5em;
            margin-top: 20px;
            animation: fadeIn 2s ease-in;
        }

        .button {
            margin-top: 30px;
            padding: 15px 25px;
            font-size: 1.2em;
            background-color: #ff6666;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .button:hover {
            background-color: #ff4040;
        }

        @keyframes titleAnimation {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }

        @keyframes heartAnimation {
            0% { transform: scale(1); }
            100% { transform: scale(1.3); }
        }

        @keyframes backgroundAnimation {
            0% { background: linear-gradient(135deg, #ff5e62, #ff9966); }
            100% { background: linear-gradient(135deg, #9966ff, #5e62ff); }
        }

        @keyframes fadeIn {
            0% { opacity: 0; }
            100% { opacity: 1; }
        }

        .message-box {
            background-color: rgba(0, 0, 0, 0.6);
            color: #fff;
            padding: 20px;
            border-radius: 10px;
            display: none;
            margin-top: 20px;
            font-size: 1.2em;
        }
    </style>
</head>
<body>
    <h1>Cảm ơn Thanh Hương rất nhiều!</h1>
    <div class="heart">❤️</div>
    <p>Vì món quà này, t sẽ rất quý m🫰🏻!</p>
    <button class="button" onclick="showMessage()">Click để xem thông điệp bí mật</button>

    <div class="message-box" id="messageBox">
        <p>Thanh Hương yêu quý!</p>
    </div>

    <script>
        function showMessage() {
            var messageBox = document.getElementById('messageBox');
            messageBox.style.display = 'block'; // Hiển thị thông điệp
            alert("Anh yêu em mãi mãi, Thanh Hương!");
        }
    </script>
</body>
</html>
